<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>travel</title>
</head>
<body>
    <h1>여행지 목록</h1>
    <ui>
        <% travelList.forEach(travel => { %>
            <li>
                <a href="/travel/<%= travel.id %>"> <%= travel.name %> </a> 
                <a href="/travel/<%= travel.id %>/edit"> 수정 </a> 
                <form action="/travel/<%= travel.id %>?_method=DELETE" method="post" style="display: inline;">
                    <button type="submit">삭제</button>
                </form>
            </li>
        <% }) %>
    </ui>
    <a href="/travel/add">여행지 추가</a>

</body>
</html>